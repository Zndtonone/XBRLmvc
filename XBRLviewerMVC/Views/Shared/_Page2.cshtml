@{
    @model List<XBRLviewerMVC.Models.FactModel>
}

@{ FactModel f, f2; string fId, fId2; string noValue = ": Einki virði"; }

@{
    fId = "IdentificationOfApprovedAnnualReport";
    f = Model.FirstOrDefault(x => x.Id == fId);
    <p class="text-left p-1" data-toggle="tooltip" title="@f?.Label">@(f?.Value ?? fId + noValue)</p>
}

@{
    fId = "ConfirmationThatAnnualReportIsPresentedInAccordanceWithRequirementsProvidedForByLegislationAnyStandardsAndRequirementsProvidedByArticlesOfAssociationOrByAgreement";
    f = Model.FirstOrDefault(x => x.Id == fId);
    <p class="text-left p-1" data-toggle="tooltip" title="@f?.Label">@(f?.Value ?? fId + noValue)</p>
}

@{
    fId = "ConfirmationThatFinancialStatementGivesTrueAndFairViewOfAssetsLiabilitiesEquityFinancialPositionAndResults";
    f = Model.FirstOrDefault(x => x.Id == fId);
    <p class="text-left p-1" data-toggle="tooltip" title="@f?.Label">@(f?.Value ?? fId + noValue)</p>
}

@{
    fId = "ManagementsStatementAboutManagementsReview";
    f = Model.FirstOrDefault(x => x.Id == fId);
    <p class="text-left p-1" data-toggle="tooltip" title="@f?.Label">@(f?.Value ?? fId + noValue)</p>
}

@{
    fId = "RecommendationForApprovalOfAnnualReportByGeneralMeeting";
    f = Model.FirstOrDefault(x => x.Id == fId);
    <p class="text-left p-1" data-toggle="tooltip" title="@f?.Label">@(f?.Value ?? fId + noValue)</p>
}

@{
    fId = "DateOfGeneralMeeting";
    f = Model.FirstOrDefault(x => x.Id == fId);

    fId2 = "AddressOfReportingEntityDistrictName";
    f2 = Model.FirstOrDefault(x => x.Id == fId2);
    <p class="text-left p-1" data-toggle="tooltip" title="@f?.Label &#10;@f2?.Label">@(f?.Value ?? fId + noValue), @(f2?.Value ?? fId2 + noValue)</p>
}

@{
    fId = "NameAndSurnameOfMemberOfExecutiveBoard";
    f = Model.FirstOrDefault(x => x.Id == fId);

    fId2 = "TitleOfMemberOfExecutiveBoard";
    f2 = Model.FirstOrDefault(x => x.Id == fId2);

    <p class="text-left p-1 font-weight-bold">Stjórn</p>
    <p class="text-left p-1" data-toggle="tooltip" title="@f?.Label &#10;@f2?.Label">
        @(f?.Value ?? fId + noValue)<br />
        <span class="font-italic">@(f2?.Value ?? fId2 + noValue)</span>
    </p>
}

@{
    fId = "NameAndSurnameOfMemberOfSupervisoryBoard";
    fId2 = "TitleOfMemberOfSupervisoryBoard";

    var memberNames = Model.Where(x => x.Id == fId)
        .Select(x => x);

    var memberTitles = Model.Where(x => x.Id == fId2)
        .Select(x => x);

    <p class="text-left p-1 font-weight-bold">Nevnd</p>

    foreach (var mn in memberNames)
    {
    <p class="text-left p-1" data-toggle="tooltip" title="@mn?.Label">@(mn?.Value ?? fId + noValue)</p>

        foreach (var mt in memberTitles)
        {
            if (mn.ContextID == mt.ContextID)
            {
                <p class="text-left p-1 font-italic" data-toggle="tooltip" title="@mt?.Label">@(mt?.Value ?? fId + noValue)</p>
            }
        }
    }
}
